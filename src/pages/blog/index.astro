---
/**
 * blog.astro - Página de listado de artículos
 *
 * Se alimenta del WP REST API. Fallback a posts mock si la API no responde.
 */
import Layout from '../../layouts/Layout.astro';
import Navbar from '../../components/Navbar.astro';
import Footer from '../../components/Footer.astro';
import WhatsAppWidget from '../../components/WhatsAppWidget.astro';

interface WPPost {
  id: number;
  title: { rendered: string };
  excerpt: { rendered: string };
  content: { rendered: string };
  date: string;
  slug: string;
  link: string;
  _embedded?: {
    'wp:featuredmedia'?: Array<{ source_url: string; alt_text: string }>;
    'wp:term'?: Array<Array<{ name: string }>>;
    author?: Array<{ name: string; avatar_urls?: Record<string, string> }>;
  };
}

const WP_API_URL = import.meta.env.WP_API_URL || '';

let posts: Array<{
  id: string;
  title: string;
  excerpt: string;
  body: string[];
  category: string;
  date: string;
  slug: string;
  image: string;
  author: string;
}> = [];

if (WP_API_URL) {
  try {
    const res = await fetch(`${WP_API_URL}/wp-json/wp/v2/posts?per_page=100&_embed&orderby=date&order=desc`, {
      headers: { 'Accept': 'application/json' },
    });
    if (res.ok) {
      const wpPosts: WPPost[] = await res.json();
      posts = wpPosts.map((p) => ({
        id: p.slug,
        title: p.title.rendered.replace(/<[^>]*>/g, ''),
        excerpt: p.excerpt.rendered.replace(/<[^>]*>/g, '').trim(),
        body: [],
        category: p._embedded?.['wp:term']?.[0]?.[0]?.name || 'Blog',
        date: new Date(p.date).toLocaleDateString('es-MX', { day: 'numeric', month: 'short', year: 'numeric' }),
        slug: p.slug,
        image: p._embedded?.['wp:featuredmedia']?.[0]?.source_url || '',
        author: p._embedded?.author?.[0]?.name || 'Portafolio Z',
      }));
    }
  } catch (e) {
    // Fallback silencioso
  }
}

// Fallback mock data
if (posts.length === 0) {
  posts = [
    {
      id: 'agencia-seo',
      title: '¿Cuándo es necesaria una agencia de SEO en México?',
      category: 'SEO',
      date: '15 Mar 2025',
      excerpt: 'Descubre en qué momento tu negocio necesita ayuda profesional para posicionarse en buscadores y atraer tráfico orgánico de calidad.',
      body: [
        'Si tu sitio ya tiene tráfico pero no convierte, puede ser que el SEO no esté atacando las palabras clave correctas.',
        'Una agencia suele ayudar cuando necesitas estrategia, auditoría técnica y contenido orientado a intención de búsqueda.',
        'La clave es medir resultados con KPIs claros (ranking, tráfico orgánico y conversiones) y ajustar el plan mes a mes.',
      ],
      slug: 'agencia-seo',
      image: '',
      author: 'Portafolio Z',
    },
    {
      id: 'seguridad-sitio',
      title: '¿Cómo mejorar la seguridad de un sitio web?',
      category: 'Seguridad',
      date: '10 Mar 2025',
      excerpt: 'Consejos esenciales de seguridad web que todo dueño de negocio debe conocer para proteger su inversión digital.',
      body: [
        'Implementa HTTPS y configura correctamente cabeceras de seguridad (HSTS, Content-Security-Policy, etc.).',
        'Mantén plugins/SDKs actualizados y realiza revisiones periódicas de vulnerabilidades.',
        'Usa backups automáticos y un plan de recuperación para reducir el impacto de un incidente.',
      ],
      slug: 'seguridad-sitio',
      image: '',
      author: 'Portafolio Z',
    },
    {
      id: 'marketing-autos-lujo',
      title: 'Marketing de autos de lujo: estrategias para clientes exigentes',
      category: 'Marketing',
      date: '05 Mar 2025',
      excerpt: 'Estrategias adaptadas para satisfacer la exigente clientela del mercado automotriz premium y generar leads de alto valor.',
      body: [
        'Enfócate en experiencia y percepción de marca: el contenido debe sentirse premium.',
        'Segmenta por intención (financiamiento, interés por modelos específicos, pruebas de manejo).',
        'Mide resultados por etapas del embudo y optimiza con campañas dirigidas a calidad de leads.',
      ],
      slug: 'marketing-autos-lujo',
      image: '',
      author: 'Portafolio Z',
    },
  ];
}

const categoryColors: Record<string, string> = {
  'SEO': 'from-cyan-500 to-blue-500',
  'Seguridad': 'from-violet-500 to-purple-500',
  'Marketing': 'from-amber-500 to-orange-500',
  'Blog': 'from-pink-500 to-rose-500',
  'Diseño': 'from-emerald-500 to-cyan-500',
  'Tecnología': 'from-indigo-500 to-violet-500',
};

function getGradient(category: string): string {
  return categoryColors[category] || 'from-primary-500 to-accent-500';
}

const featured = posts[0];
const rest = posts.slice(1);
const categories = [...new Set(posts.map(p => p.category))];

// Group posts by category (excluding featured)
const postsByCategory = categories.map((cat) => ({
  category: cat,
  gradient: getGradient(cat),
  posts: rest.filter((p) => p.category === cat),
})).filter((g) => g.posts.length > 0);
---

<Layout title="Blog | Portafolio Z" description="Artículos sobre diseño web, SEO, marketing digital y tecnología para emprendedores y negocios en México.">
  <Navbar />

  <!-- HERO -->
  <section class="relative pt-32 pb-16 overflow-hidden">
    <div class="absolute inset-0 bg-gradient-to-br from-dark-900 via-purple-950/40 to-dark-900"></div>
    <div class="absolute inset-0 bg-[radial-gradient(ellipse_at_top,rgba(139,92,246,0.15),transparent_60%)]"></div>
    <div class="absolute top-20 right-20 w-72 h-72 bg-purple-500/10 rounded-full blur-3xl"></div>
    <div class="absolute bottom-0 left-10 w-64 h-64 bg-cyan-500/10 rounded-full blur-3xl"></div>

    <div class="relative z-10 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <nav class="flex gap-2 text-sm text-white/60 mb-6" data-animate>
        <a href="/" class="hover:text-white transition-colors">Inicio</a>
        <span>/</span>
        <span class="text-white">Blog</span>
      </nav>

      <div class="max-w-3xl">
        <h1 class="text-4xl sm:text-5xl lg:text-6xl font-bold text-white mb-6" data-animate>
          Blog para <span class="gradient-text">emprendedores</span>
        </h1>
        <p class="text-xl text-white/60" data-animate>
          Ideas prácticas sobre diseño web, SEO, marketing digital y tecnología para hacer crecer tu negocio.
        </p>
      </div>

    </div>
  </section>

  <!-- SEARCH & CATEGORIES -->
  <section class="bg-gray-50 pt-8 pb-12">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8" data-animate>
      <div class="bg-white border border-gray-200 rounded-2xl p-6 md:p-8 shadow-sm">
        <div class="flex flex-col md:flex-row md:items-center gap-6">
          <!-- Search -->
          <div class="flex-1 relative">
            <svg class="absolute left-4 top-1/2 -translate-y-1/2 w-5 h-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
            </svg>
            <input
              type="text"
              id="blog-search"
              placeholder="Buscar artículos por título, tema o palabra clave..."
              class="w-full bg-gray-50 border border-gray-200 rounded-xl pl-12 pr-4 py-3.5 text-gray-900 placeholder-gray-400 text-sm focus:outline-none focus:border-primary-500 focus:ring-2 focus:ring-primary-500/20 focus:bg-white transition-all"
            />
          </div>
          <!-- Category filters -->
          <div class="flex flex-wrap gap-2">
            <button class="blog-filter active bg-primary-500/10 border border-primary-500/30 text-primary-600 text-sm font-medium px-4 py-2.5 rounded-full cursor-pointer transition-all" data-category="all">
              Todos
            </button>
            {categories.map((cat) => (
              <button class="blog-filter bg-gray-50 border border-gray-200 text-gray-500 text-sm font-medium px-4 py-2.5 rounded-full hover:bg-primary-50 hover:text-primary-600 hover:border-primary-300 cursor-pointer transition-all" data-category={cat}>
                {cat}
              </button>
            ))}
          </div>
        </div>
        <!-- Results count -->
        <p class="text-gray-400 text-xs mt-4" id="blog-results-count">{posts.length} artículos encontrados</p>
      </div>
    </div>
  </section>

  <!-- FEATURED POST -->
  {featured && (
    <section class="bg-gray-50 pb-12" id="featured-section">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8" data-animate>
        <p class="text-gray-400 text-xs font-semibold uppercase tracking-widest mb-4">Artículo destacado</p>
        <a href={`/${featured.slug}`} class="blog-card group block rounded-3xl overflow-hidden bg-white border border-gray-200 hover:border-gray-300 hover:shadow-xl transition-all duration-300" data-category={featured.category} data-title={featured.title.toLowerCase()}>
          <div class="grid md:grid-cols-2">
            <!-- Image -->
            <div class="relative h-64 md:h-auto md:min-h-[360px] overflow-hidden">
              {featured.image ? (
                <img src={featured.image} alt={featured.title} class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-700" loading="lazy" />
              ) : (
                <div class={`w-full h-full bg-gradient-to-br ${getGradient(featured.category)}`}>
                  <div class="absolute inset-0 bg-[radial-gradient(circle_at_30%_40%,rgba(255,255,255,0.15),transparent_60%)]"></div>
                  <div class="absolute bottom-6 left-6">
                    <span class="text-6xl opacity-20">📝</span>
                  </div>
                </div>
              )}
              <div class="absolute top-5 left-5">
                <span class="bg-white/80 backdrop-blur-md text-gray-800 text-xs font-bold px-3 py-1.5 rounded-full border border-white/40 uppercase tracking-wider shadow-sm">
                  Destacado
                </span>
              </div>
            </div>
            <!-- Content -->
            <div class="p-8 md:p-10 flex flex-col justify-center">
              <div class="flex items-center gap-3 mb-4">
                <span class={`bg-gradient-to-r ${getGradient(featured.category)} text-white text-xs font-bold px-3 py-1 rounded-full`}>
                  {featured.category}
                </span>
                <time class="text-gray-400 text-sm">{featured.date}</time>
              </div>
              <h2 class="text-2xl md:text-3xl font-bold text-gray-900 mb-4 group-hover:text-primary-600 transition-colors">
                {featured.title}
              </h2>
              <p class="text-gray-500 leading-relaxed mb-6">{featured.excerpt}</p>
              <div class="flex items-center justify-between">
                <span class="text-gray-400 text-sm">{featured.author}</span>
                <span class="flex items-center gap-2 text-primary-600 text-sm font-medium group-hover:gap-3 transition-all">
                  Leer artículo
                  <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
                  </svg>
                </span>
              </div>
            </div>
          </div>
        </a>
      </div>
    </section>
  )}

  <!-- POSTS BY CATEGORY -->
  {postsByCategory.map((group) => (
    <section class="bg-gray-50 pb-16 blog-category-section" data-section-category={group.category}>
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex items-center gap-4 mb-8" data-animate>
          <div class={`h-8 w-1.5 rounded-full bg-gradient-to-b ${group.gradient}`}></div>
          <div>
            <h2 class="text-2xl font-bold text-gray-900">{group.category}</h2>
            <p class="text-gray-400 text-sm">{group.posts.length} artículo{group.posts.length !== 1 ? 's' : ''}</p>
          </div>
        </div>
        <div class="grid sm:grid-cols-2 lg:grid-cols-3 gap-8" data-animate>
          {group.posts.map((post) => (
            <a
              href={`/${post.slug}`}
              class="blog-card group rounded-2xl overflow-hidden bg-white border border-gray-200 hover:border-gray-300 hover:-translate-y-2 hover:shadow-xl transition-all duration-300"
              data-category={post.category}
              data-title={post.title.toLowerCase()}
            >
              <div class="relative h-52 overflow-hidden">
                {post.image ? (
                  <img src={post.image} alt={post.title} class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500" loading="lazy" />
                ) : (
                  <div class={`w-full h-full bg-gradient-to-br ${getGradient(post.category)} opacity-80`}>
                    <div class="absolute inset-0 bg-[radial-gradient(circle_at_30%_50%,rgba(255,255,255,0.1),transparent_60%)]"></div>
                  </div>
                )}
                <div class="absolute top-4 left-4">
                  <span class={`bg-gradient-to-r ${getGradient(post.category)} text-white text-xs font-bold px-3 py-1 rounded-full shadow-lg`}>
                    {post.category}
                  </span>
                </div>
              </div>
              <div class="p-6">
                <div class="flex items-center gap-3 mb-3">
                  <time class="text-gray-400 text-xs">{post.date}</time>
                  <span class="text-gray-300">·</span>
                  <span class="text-gray-400 text-xs">{post.author}</span>
                </div>
                <h3 class="text-lg font-bold text-gray-900 mb-3 group-hover:text-primary-600 transition-colors line-clamp-2">
                  {post.title}
                </h3>
                <p class="text-gray-500 text-sm leading-relaxed line-clamp-2">
                  {post.excerpt}
                </p>
                <div class="flex items-center gap-2 mt-5 text-primary-600 text-sm font-medium">
                  <span>Leer artículo</span>
                  <svg class="w-4 h-4 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
                  </svg>
                </div>
              </div>
            </a>
          ))}
        </div>
      </div>
    </section>
  ))}

  <!-- FULL ARTICLES -->
  <section class="bg-gray-50 pb-24">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">

      <!-- Full articles (for mock/fallback) -->
      <div class="space-y-8">
        {posts.filter(p => p.body.length > 0).map((post) => (
          <article id={`post-${post.id}`} class="blog-article scroll-mt-32 bg-dark-800/50 border border-white/10 rounded-3xl overflow-hidden" data-category={post.category} data-title={post.title.toLowerCase()}>
            <!-- Article header -->
            <div class={`h-2 bg-gradient-to-r ${getGradient(post.category)}`}></div>
            <div class="p-7 md:p-10">
              <div class="flex flex-col sm:flex-row sm:items-center gap-3 mb-5">
                <span class={`bg-gradient-to-r ${getGradient(post.category)} text-white text-xs font-bold px-3 py-1 rounded-full w-fit`}>
                  {post.category}
                </span>
                <time class="text-white/40 text-sm">{post.date}</time>
                <span class="text-white/20 hidden sm:inline">·</span>
                <span class="text-white/40 text-sm">{post.author}</span>
              </div>
              <h2 class="text-2xl md:text-3xl font-bold text-white mb-4">{post.title}</h2>
              <p class="text-white/60 text-lg leading-relaxed mb-6">{post.excerpt}</p>
              <div class="space-y-4 border-t border-white/10 pt-6">
                {post.body.map((p) => (
                  <p class="text-white/70 leading-relaxed">{p}</p>
                ))}
              </div>
              <div class="mt-8 pt-6 border-t border-white/10 flex items-center justify-between">
                <a href="#" onclick="window.scrollTo({top:0,behavior:'smooth'});return false" class="inline-flex items-center gap-2 text-cyan-400 hover:text-cyan-300 text-sm font-medium transition-colors">
                  <svg class="w-4 h-4 rotate-180" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
                  </svg>
                  Volver arriba
                </a>
                <a href="https://wa.me/5215564721547?text=Hola%2C%20vi%20su%20*Blog*%20y%20me%20gustar%C3%ADa%20cotizar%20un%20servicio.%20%C2%BFMe%20pueden%20asesorar%3F" target="_blank" rel="noopener" class="wa-lead inline-flex items-center gap-2 gradient-bg text-white text-sm font-semibold px-5 py-2 rounded-full hover:opacity-90 transition-all" data-plan="Necesitas ayuda" data-price="" data-category="Blog">
                  ¿Necesitas ayuda?
                </a>
              </div>
            </div>
          </article>
        ))}
      </div>
    </div>
  </section>

  <!-- CTA -->
  <section class="relative py-20 overflow-hidden">
    <div class="absolute inset-0 bg-gradient-to-r from-primary-600 via-accent-600 to-pink-600"></div>
    <div class="absolute inset-0 bg-black/20"></div>
    <div class="relative z-10 max-w-4xl mx-auto px-4 text-center" data-animate>
      <h2 class="text-3xl md:text-4xl font-bold text-white mb-4">¿Listo para transformar tu presencia digital?</h2>
      <p class="text-white/80 text-lg mb-8">Aplica lo que aprendes en el blog o déjanos hacerlo por ti.</p>
      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <a href="https://wa.me/5215564721547?text=Hola%2C%20vi%20su%20*Blog*%20y%20me%20gustar%C3%ADa%20cotizar%20un%20servicio.%20%C2%BFMe%20pueden%20asesorar%3F" target="_blank" rel="noopener" class="wa-lead bg-white text-primary-600 font-bold px-8 py-4 rounded-full text-lg hover:bg-white/90 transition-all hover:scale-105 shadow-xl" data-plan="Solicitar Cotización" data-price="" data-category="Blog">
          Solicitar Cotización
        </a>
        <a href="https://wa.me/5215564721547?text=Hola%2C%20vi%20su%20*Blog*%20y%20me%20gustar%C3%ADa%20cotizar%20un%20servicio.%20%C2%BFMe%20pueden%20asesorar%3F" target="_blank" rel="noopener" class="wa-lead border-2 border-white text-white font-semibold px-8 py-4 rounded-full text-lg hover:bg-white/10 transition-all" data-plan="WhatsApp" data-price="" data-category="Blog">
          WhatsApp
        </a>
      </div>
    </div>
  </section>

  <Footer />
  <WhatsAppWidget />
</Layout>

<script>
function initBlogFilters() {
  const searchInput = document.getElementById('blog-search') as HTMLInputElement;
  const filterBtns = document.querySelectorAll('.blog-filter');
  const cards = document.querySelectorAll('.blog-card');
  const articles = document.querySelectorAll('.blog-article');
  const categorySections = document.querySelectorAll('.blog-category-section');
  const resultsCount = document.getElementById('blog-results-count');
  const featuredSection = document.getElementById('featured-section');

  let activeCategory = 'all';

  function applyFilters() {
    const query = searchInput?.value.toLowerCase().trim() || '';
    let visible = 0;

    cards.forEach((card) => {
      const el = card as HTMLElement;
      const cat = el.dataset.category || '';
      const title = el.dataset.title || '';
      const matchesCat = activeCategory === 'all' || cat === activeCategory;
      const matchesSearch = !query || title.includes(query);
      if (matchesCat && matchesSearch) {
        el.style.display = '';
        visible++;
      } else {
        el.style.display = 'none';
      }
    });

    articles.forEach((article) => {
      const el = article as HTMLElement;
      const cat = el.dataset.category || '';
      const title = el.dataset.title || '';
      const matchesCat = activeCategory === 'all' || cat === activeCategory;
      const matchesSearch = !query || title.includes(query);
      el.style.display = (matchesCat && matchesSearch) ? '' : 'none';
    });

    // Hide category sections if no visible cards inside
    categorySections.forEach((section) => {
      const el = section as HTMLElement;
      const sectionCat = el.dataset.sectionCategory || '';
      const matchesCat = activeCategory === 'all' || sectionCat === activeCategory;
      const visibleCards = el.querySelectorAll('.blog-card:not([style*="display: none"])');
      el.style.display = (matchesCat && visibleCards.length > 0) ? '' : 'none';
    });

    // Hide featured section if featured post is hidden
    if (featuredSection) {
      const featuredCard = featuredSection.querySelector('.blog-card') as HTMLElement;
      featuredSection.style.display = featuredCard && featuredCard.style.display !== 'none' ? '' : 'none';
    }

    if (resultsCount) {
      resultsCount.textContent = `${visible} artículo${visible !== 1 ? 's' : ''} encontrado${visible !== 1 ? 's' : ''}`;
    }
  }

  // Category filter clicks
  filterBtns.forEach((btn) => {
    btn.addEventListener('click', () => {
      filterBtns.forEach((b) => {
        b.classList.remove('active', 'bg-cyan-500/20', 'border-cyan-500/30', 'text-cyan-400');
        b.classList.add('bg-white/5', 'border-white/10', 'text-white/60');
      });
      btn.classList.add('active', 'bg-cyan-500/20', 'border-cyan-500/30', 'text-cyan-400');
      btn.classList.remove('bg-white/5', 'border-white/10', 'text-white/60');
      activeCategory = (btn as HTMLElement).dataset.category || 'all';
      applyFilters();
    });
  });

  // Search input
  searchInput?.addEventListener('input', applyFilters);
}

initBlogFilters();
document.addEventListener('astro:page-load', initBlogFilters);
</script>

<script>
  document.addEventListener('astro:page-load', () => {
    document.querySelectorAll('.wa-lead').forEach((btn) => {
      btn.addEventListener('click', () => {
        const el = btn as HTMLElement;
        if (typeof gtag === 'function') {
          gtag('event', 'generate_lead', {
            event_category: 'WhatsApp',
            plan_name: el.dataset.plan || '',
            plan_price: el.dataset.price || '',
            category: el.dataset.category || '',
            source_page: 'blog',
          });
        }
      });
    });
  });
</script>
